/**
 * Route helpers for Settings → Appearance.
 * Keep it tiny; expand later as needed.
 */

export const edit = () => ({
  url: '/settings/appearance',
});

/** If you prefer a plain URL helper, this also works:
export const editUrl = () => '/settings/appearance';
*/
